/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "dancingastronaut.com",
      "lh3.googleusercontent.com",
      "api.qrserver.com"
    ],
  },
};

module.exports = nextConfig;
